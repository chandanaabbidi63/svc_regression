# TODO

- [x] Identify existing Streamlit entrypoint (`app.py`).
- [x] Add Streamlit config directory `.streamlit/`.
- [x] Create `.streamlit/config.toml`.
- [x] Add `README_streamlit.md` with run instructions.
- [ ] Run `pip install -r requirements.txt`.
- [ ] Run `streamlit run app.py` and verify the UI renders.

